#include <stdio.h>
#include <stdlib.h>
#include "deque_str.h"

int main() {
    // deque_str* d = dstr_create(10);
    // dstr_push_back(d, "Hello");
    // dstr_push_front(d, "World");
    
    // char* s1 = dstr_pop_front(d); // "World"
    // printf("%s\n", s1);
    // free(s1);
    
    // char* s2 = dstr_pop_back(d); // "Hello"
    // printf("%s\n", s2);
    // free(s2);
    
    // dstr_destroy(d);
}